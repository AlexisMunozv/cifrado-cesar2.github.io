window.cipher = {
     encode: function(offset, string){
       let text1=
    for (let i=0, fin=i.length;i<fin; i++){
      
      if (32==="") {
      
      }
      
      else{

      }

    }
    

      

    };

     decode: function(offset,string){
     };
};
